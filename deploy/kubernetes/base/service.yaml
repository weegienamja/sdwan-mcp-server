apiVersion: v1
kind: Service
metadata:
  name: cisco-vmanage-mcp
  labels:
    app.kubernetes.io/name: cisco-vmanage-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cisco-vmanage-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP